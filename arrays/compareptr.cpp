# include <iostream>
using namespace std;

int main(void){

    int* ptr1;
    int* ptr2;

    cout << ptr1 << endl;
    cout << ptr1 << endl;

    cout << (ptr1 < ptr2) << endl; // yes agar hoga then well get 1 or else 0

    // so in genral we can use all the realtional ops like lessthan not equal to etc
    
    
}